part of openapi.api;

class ReverseGeocodingResponse {
  /* UUID for the request, generated by RUMBO at the time of the request. */
  String requestId = null;

  ServiceContext service = null;

  List<PlaceItem> results = [];
  ReverseGeocodingResponse();

  @override
  String toString() {
    return 'ReverseGeocodingResponse[requestId=$requestId, service=$service, results=$results, ]';
  }

  ReverseGeocodingResponse.fromJson(Map<String, dynamic> json) {
    if (json == null) return;
    requestId = json['requestId'];
    service = (json['service'] == null)
        ? null
        : ServiceContext.fromJson(json['service']);
    results = (json['results'] == null)
        ? null
        : PlaceItem.listFromJson(json['results']);
  }

  Map<String, dynamic> toJson() {
    Map<String, dynamic> json = {};
    if (requestId != null) json['requestId'] = requestId;
    if (service != null) json['service'] = service;
    if (results != null) json['results'] = results;
    return json;
  }

  static List<ReverseGeocodingResponse> listFromJson(List<dynamic> json) {
    return json == null
        ? <ReverseGeocodingResponse>[]
        : json
            .map((value) => ReverseGeocodingResponse.fromJson(value))
            .toList();
  }

  static Map<String, ReverseGeocodingResponse> mapFromJson(
      Map<String, dynamic> json) {
    var map = Map<String, ReverseGeocodingResponse>();
    if (json != null && json.isNotEmpty) {
      json.forEach((String key, dynamic value) =>
          map[key] = ReverseGeocodingResponse.fromJson(value));
    }
    return map;
  }

  // maps a json object with a list of ReverseGeocodingResponse-objects as value to a dart map
  static Map<String, List<ReverseGeocodingResponse>> mapListFromJson(
      Map<String, dynamic> json) {
    var map = Map<String, List<ReverseGeocodingResponse>>();
    if (json != null && json.isNotEmpty) {
      json.forEach((String key, dynamic value) {
        map[key] = ReverseGeocodingResponse.listFromJson(value);
      });
    }
    return map;
  }
}
